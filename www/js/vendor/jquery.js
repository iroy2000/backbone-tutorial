// This is a jQuery stub for 'tricking' the built tool.
// Since we are using require-jquery, the jQuery namespace should be there already
// we just need to return the jQuery object for this 'module'.
define( "jquery", [], function () { return jQuery; } );
